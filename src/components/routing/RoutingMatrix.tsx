// Komponente: RoutingMatrix — Routing-Matrix (Inputs → Busse → Outputs)

/** Routing-Matrix als interaktives Grid (Inputs als Zeilen, Busse als Spalten) */
interface RoutingMatrixProps {}

function RoutingMatrix(_props: RoutingMatrixProps) {
  // TODO: Grid-Layout (Inputs vertikal, Busse horizontal)
  // TODO: Kreuzungspunkte als Toggle-Buttons
  // TODO: Farben: Cyan für A-Busse, Orange für B-Busse
  return (
    <div className="bg-inox-panel border border-[rgba(255,255,255,0.05)] rounded-[5px] p-3">
      {/* TODO: Routing-Matrix Grid */}
    </div>
  );
}

export default RoutingMatrix;
